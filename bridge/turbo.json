{
  "$schema": "https://turborepo.dev/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "command": ["go", "build", "-o", "bridge", "."],
      "outputs": ["bridge"]
    },
    "test": {
      "command": ["go", "test", "-race", "./..."]
    }
  }
}
